node_modules/
provinces.geojson
.env
*.bat
pm2.5/

# uploads — เก็บ folder แต่ไม่เก็บไฟล์ข้างใน
uploads/*
!uploads/.gitkeep

# ไฟล์ที่ไม่จำเป็น
*.log
* - Copy.*

# ฐานข้อมูล local — ห้าม commit (ทำให้ repo บวมและชนกัน)
*.db
